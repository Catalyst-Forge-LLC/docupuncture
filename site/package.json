{
	"name": "docupuncture-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "node ../scripts/ensure-lease.mjs docupuncture-site 5188 && filepress dev --host 0.0.0.0",
		"build": "filepress build",
		"preview": "filepress preview",
		"check": "filepress check",
		"ship": "pnpm build && wrangler pages deploy build --project-name=docupuncture",
		"deploy": "pnpm ship"
	},
	"devDependencies": {
		"getfilepress": "^0.1.9",
		"wrangler": "^4.28.0"
	}
}
